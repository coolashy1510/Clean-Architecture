import { ErrorDataModel } from './errorModel';

/**
 * This is a map of business error codes to error messages.
 * @author Ashwin Kumar (n499160)
 */
const businessErrorMap: ErrorDataModel = {
  OLCI_SERVICE_NAME_1001: 'UNEXPECTED_BUSINESS_ERROR'
};

export default businessErrorMap;
